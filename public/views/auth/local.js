// Generated by CoffeeScript 1.6.3
(function() {
  var __bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; };

  define(['q', 'odo/auth'], function(Q, auth) {
    var SigninLocal;
    return SigninLocal = (function() {
      function SigninLocal() {
        this.close = __bind(this.close, this);
        this.activate = __bind(this.activate, this);
        this.canActivate = __bind(this.canActivate, this);
      }

      SigninLocal.prototype.canActivate = function() {
        var dfd,
          _this = this;
        dfd = Q.defer();
        auth.getUser().then(function(user) {
          return dfd.resolve({
            redirect: '#'
          });
        }).fail(function(err) {
          return dfd.resolve(true);
        });
        return dfd.promise;
      };

      SigninLocal.prototype.activate = function(options) {
        return this.dialog = options.dialog, options;
      };

      SigninLocal.prototype.close = function() {
        return this.dialog.close();
      };

      return SigninLocal;

    })();
  });

}).call(this);
